# CV

Drop your CV here as `joaquin-leimeter-cv.pdf`.

The header Resume button links to `/cv/joaquin-leimeter-cv.pdf` (configured in
`content/src/markdown/site.md` → `cv.path`). Until the PDF is added, the link
will 404. To change the filename, edit `cv.path` in `site.md` and rebuild content.
